# Web app configuration (optional). Copy to .env.local to override.
# Base URL of the ASTROBSM API. Defaults to http://localhost:4000/api/v1
# In the single-project Vercel deployment the API is same-origin, so set:
#   VITE_API_URL=/api/v1
VITE_API_URL=http://localhost:4000/api/v1
